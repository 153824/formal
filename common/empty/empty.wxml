<!--common/empty/empty.wxml-->
<view wx:if="{{ type === 'default' && isShow }}" class="empty-wrapper {{ customClass ? customClass : '' }}">
    <view class="title">{{ title }}</view>
    <image src="https://ihola.luoke101.com/icon%40icon-empty-type-1.png"></image>
</view>
