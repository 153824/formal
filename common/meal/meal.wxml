<!--common/meal/meal.wxml-->
<view wx:if="{{ isShow }}" class="meal">
    <view class="meal-info">
        <image wx:if="{{ icon }}" class="meal-icon" src="{{ icon }}"></image>
        <text>{{ title }}</text>
    </view>
    <view class="meal-detail">
        {{ subtitle }}
    </view>
</view>
