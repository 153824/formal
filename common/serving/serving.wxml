<view class="serving" bindtap="goToServer" style="bottom:{{ bottom }};right: {{ right }};visibility: {{visibility === 'visible' ? 'visible' : 'hidden' }}">
    <image src="{{ image }}" style="height: 50px;width: 50px;"></image>
</view>
