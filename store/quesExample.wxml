<!--store/quesExample.wxml-->
<view class="title">{{stem}}</view>
<view class="options">
  <block wx:for="{{options}}" wx:key="unique">
    <view class="flex item">
      <text class="txt">{{item.txt}}</text>
      <text class="flex-main msg">{{item.msg}}</text>
    </view>
  </block>
</view>